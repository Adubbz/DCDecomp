#pragma once

/* Declarations only; reassembled from ref/asm/split/main/rand.s. */

extern "C" {

int rand(void);
void srand(unsigned int seed);
double atof(const char *text);
int atoi(const char *text);
void exit__2(int status);
}
